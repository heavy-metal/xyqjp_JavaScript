.searchBar {
  height: 90rpx;
  padding: 10rpx 20rpx;
  background-color: var(--themeColor);
}
.searchBar navigator {
  height: 100%;
  display: flex;
  align-items: center;
  border-radius: 30rpx;
  background-color: rgba(0, 0, 0, 0.08);
  color: var(--lineColor);
  font-size: 24rpx;
}
